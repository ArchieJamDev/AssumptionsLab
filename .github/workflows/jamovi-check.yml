name: Validar Módulo jamovi

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  jamovi-check:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Descargar el código del repositorio
        uses: actions/checkout@v4

      - name: Configurar R en la máquina virtual
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: 'release'

      - name: Configurar dependencias del sistema de Linux
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libssl-dev libxml2-dev

      - name: Instalar jmvtools y dependencias del módulo
        run: |
          Rscript -e "install.packages(c('remotes', 'rcmdcheck'), repos = 'https://r-project.org')"
          Rscript -e "remotes::install_github('jamovi/jmvtools')"
          Rscript -e "remotes::install_deps(dependencies = TRUE)"

      - name: Compilar y verificar el módulo de jamovi
        run: |
          Rscript -e "jmvtools::check()"
